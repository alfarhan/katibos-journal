#pragma once
// Host stub for the NimBLE-Arduino library. KeyboardScreen.cpp only #includes
// this header (the actual BLE work goes through BleKeyboard.h's no-op class);
// no NimBLE* symbols are referenced directly, so an empty header suffices.
